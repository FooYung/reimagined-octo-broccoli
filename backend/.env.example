# Port the Express server listens on
PORT=3000

# Secret used to sign JWTs (Phase 3+) — replace in production
JWT_SECRET=change-me-in-production

# SQLite database location (Phase 2+)
DATABASE_URL="file:./dev.db"
